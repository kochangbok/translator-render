{
  "name": "translator-render",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "icons": "python3 scripts/generate-icons.py",
    "dev": "vite build --watch",
    "build": "vite build",
    "pack": "npm run build && node scripts/pack-extension.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.17",
    "@types/node": "^25.3.3",
    "typescript": "^5.6.3",
    "vite": "^6.2.0",
    "vitest": "^3.0.8"
  }
}
